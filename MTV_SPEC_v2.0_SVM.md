# MTV_SPEC_v2.0_SVM.md — SVM(Session Virtual Memory)のMCP実装と「ユーザー利益」検証仕様

> **Executorへ**: T1の実領収書（セッション7d28a385、$57.01、clean 16.5%）を受領した。良い仕事だった。本書はその後ファウンダー側で行われた理論進展のすべてを含む正式な次期指示であり、v1.1を**置換ではなく拡張**する（v1.1の§2凍結数式・H1–H7・D1–D3はすべて有効）。旧T2/T3は本書のタスク列に再配置した。
> **本書の中心命題**: 「SVMをMCPサーバー＋フックとして実装したとき、ユーザーは**行動変容ゼロで純利益**を得るか」。本書の検証は **No と判定できるように**設計されている。Noなら成果報酬価格は成立せず、製品は監査（Stage 1）止まりとなる。それを最小費用で先に知ることが目的である。
> v2.0 — 2026-06-12 / 凍結規律はv1.1と同じ（数式・反証条件は実行前凍結、結果は§9に追記専用）

---

## §0. 理論進展の要約（Executorが文脈ゼロから追うための5段）

1. **領収書の最大行の正体**: clean 16.5%（例: 1ブロックが291回再送）は「ブロックの寿命管理機構が存在しない」ことの帰結。コンテキストウィンドウがデータベースとして誤用されている。
2. **素朴な排出は損をする**: Anthropicのプロンプトキャッシュは厳密にプレフィックス一致。中間ブロックの除去は後続全体をキャッシュミス化し、0.1倍単価だったトークンを1.25倍で再請求させる。本セッションはプロンプト側78.3Mのうち77.2M（98.6%）がキャッシュ読込＝「家賃レジーム」。**排出ヒューリスティクス（旧CLF案）は棄却済み。**
3. **解はSVM**: セッションに番地を振り（Clean MCPがrepoに振ったように）、文脈には本体でなくスタブ＋番地を住まわせ、必要時に`ctx_read`でデマンドページング。ページャは**フック**に置く（LLMトークンを判断に使わない——MemGPTの死因の回避）。排出は「無料窓」（compaction・セッション再開・どのみちミスする瞬間）に限定する。
4. **3つの発明が推定器**: ①Context-MRC（再利用距離→ミス率曲線、最適常駐量の解析解）②出自インデックス（セッション＝ビルドグラフ、検証ゲート付き近似メモ化、増分リプレイ）③前方再利用蒸留（ρ̂の学習、ラベルは反実仮想リプレイ）。本書ではL0（ヒューリスティック）版のみ実装する。
5. **本書の問い**: 上記をMCPに落としたとき、スキーマ税・フォルト往復・モデル順守という3つの新コストを差し引いて、なおユーザーが得をするか。

---

## §1. ユーザー利益（UB）の定義 — 検証の主役

UBはベクトルで定義し、**全条件の同時成立**のみを合格とする：

```
UB = 1  ⟺  ΔCost$    ≥ θ_c (= 15%)        … 実請求額の削減（トークン数ではなく$）
        ∧  ΔSuccess  ≥ −θ_s (= 2pt)        … 接地成功率の非劣化（テスト合格）
        ∧  ΔLatency  ≤ +θ_l (= 10%)        … ターン平均ウォールクロック
        ∧  行動変容   = 0                    … ユーザーの操作・習慣の変更を要求しない
        ∧  最大後悔   ≤ θ_r (= 2%)          … どの単一セッションでも実費増がこれ以下（無害性）
```

**MCP化が利益を壊しうる3経路と、対応する検証**（本書はこの3つを測るために存在する）:

| 失敗経路 | 内容 | 測る場所 |
|---|---|---|
| スキーマ税 | MCPツール定義自体が毎ターン文脈を食う | 設計法則1＋V-D |
| フォルト費 | スタブ化したブロックの呼戻し（ツール往復＋再入場）が保持より高くつく | 数式3.2＋V-A/V-B |
| モデル順守 | モデルがスタブを正しく扱えない（ctx_readしない／中身を幻覚する） | V-C（フォルト想起率） |

---

## §2. 全体アーキテクチャ

```
[Claude Code セッション]
   │
   ├── フック層（ページャ。LLMトークン消費ゼロ、全てfail-open）
   │     PostToolUse      : ツール結果を全文Storeへ保存 → π_b>0 なら本体を
   │                        スタブに置換して文脈へ（誕生時判定。遡及置換は禁止）
   │     SessionStart     : matcher=compact|resume（無料窓）→ 深部排出＋温イメージ復元
   │     SubagentStart/Task: ペイロード→番地渡し（v2.1以降、V-E）
   │     UserPromptSubmit : 末尾追記のみ（排出通知等）。プレフィックス変更は全フックで禁止
   │
   ├── MCPサーバー（想起器官。最小スキーマ）
   │     ctx_read(handle, range?)  : デマンドページング（全文 or 行範囲）
   │     ctx_search(query)         : セッションストア全文検索（FTS5）
   │     ※ツールは2つのみ。スキーマ合計 < 400トークン（計測テスト必須）
   │
   └── Session Store（SQLite。mtv-auditと同居）
         blocks      : 内容アドレス(CAS: sha256, body, size, birth_turn, type)
         access_log  : 因果参照ログ → 再利用距離 → MRC（発明①の生データ）
         edges       : 出自辺（発明②の骨格。v2.0ではスキーマのみ定義、辺生成はL0）
         ledger      : 追記専用（v1.1台帳と互換。スタブ判定・排出・フォルトを全記録）
```

**スタブ形式（凍結・V-Cで文言A/B可）**:
```
[SVM:paged h=b3f2a1 type=tool_result tool=Grep size=3.4k born=t31]
要約: ChatContext.tsx のimport群とprops型定義（≤2行、決定的生成）
全文が必要なら ctx_read("b3f2a1")。部分: ctx_read("b3f2a1", lines="1-40")
```

**設計5法則（恒久・テストで強制）**:
1. **スキーマ最小**: 自前MCPの定義 < 400トークン。
2. **末尾限定**: いかなる操作もプレフィックスを変更しない（中間排出禁止則）。
3. **番地は常に可視**: ページアウトしても「何が存在し、どう呼ぶか」は文脈に残る（未知の未知を作らない）。
4. **深部排出は無料窓のみ**: compaction／再開イベントに相乗り。
5. **fail-open**: Store障害・タイムアウト時は原文をそのまま通す。エラーで品質を落とさない。

---

## §3. 確定数式集 v2.0（凍結）

単価は価格表設定ファイル（price_table_version必須）から。既定: 入力=1, キャッシュ読込 p_read=0.1, キャッシュ書込 p_write=1.25, 出力 p_out=5（入力単価比）。

### 3.1 Rent-or-Rebuild 排出律（v1.1拡張・凍結）
```
keep(b)  = R̂_b · s_b · p_read                          （家賃）
evict(b) = 1[中間] · S_b · (p_write − p_read) + ρ̂_b · f_b （再建費＋フォルト期待費）
排出 ⟺ keep > evict
系1: 中間単独排出はほぼ常に不成立（S_b/s_b が大きいため）
系2: 無料窓では再建費項=0 → 0.1·R̂_b·s_b > ρ̂_b·f_b で大量排出が解禁
```

### 3.2 スタブ収支 π_b（新規 — MCP化のミクロ利益条件）
誕生時にスタブ化すべきかの判定。σ=スタブ長, c_call=呼出オーバーヘッド（ツール呼出の出力トークン×p_out＋枠組）, n̂_b=期待フォルト回数, R̂'_b=フォルト後の平均残存ターン:
```
cost_keep(b) = s_b · (p_write + p_read · R̂_b)
cost_stub(b) = σ · (p_write + p_read · R̂_b)
             + n̂_b · [ c_call + s_b · (p_write + p_read · R̂'_b) ]
π_b = cost_keep − cost_stub      スタブ化 ⟺ π_b > 0
```
**実例（領収書ブロック#2: s_b≈3,440tok, 残存291ターン）**: σ=80, n̂=3, R̂'=20, c_call=300 でも
cost_keep≈104.4k, cost_stub≈2.4k+3×(0.3k+11.2k)≈36.9k → **π≈+67.5k token単位（約65%削減）**。
3回も全文呼戻しが起きてなお大幅黒字——これがMCP化の利益の源泉の定量形。逆に小ブロック・高頻度参照では π_b<0 となり**スタブ化しない**（無害性の根拠）。

### 3.3 Context-MRC（発明①）
因果参照（定義は§4 V-A手順1）に基づく再利用距離 rd の分布から:
```
MR_ctx(c) = P(rd > c)        膝 c* = 曲率最大点（または限界フォルト率がλ(U)を割る点）
```
ダイヤル対応: saver→c<c*, balanced→c≈c*, optimizer→c>c*。**λダイヤルが初めて理論的定義を得る。**

### 3.4 推定器（L0・本書スコープ）
- **因果参照判定L0**: ブロック内容と、その後のassistant出力・ツール入力との正規化字句重複 ≥ τ_ref（既定0.3、較正対象）。
- **n̂_b, ρ̂_b L0**: ブロック型別の事前分布を**T1の実ログから推定**（例: test出力は3ターン以内に参照されその後死ぬ／file_read系は編集対象なら高頻度…）。型×サイズ×直近参照の3特徴のルックアップ表。
- **R̂_b L0**: 過去セッション長の生存曲線の中央値（無ければ固定値60ターン）。
- L1（リプレイ真値）・学習版（発明③）はv2.1以降。**L0とoracleの差分がそのまま発明③の価値の見積りになる**（V-Aが出力する）。

---

## §4. 検証プロトコル（V-A〜V-E、費用順）

### V-A シミュレーション監査（$0・最優先・Executorが今日着手）
入力: T1のJSONL（追加セッションがあれば併用）。LLM呼び出しゼロ。
1. ブロック分解＋因果参照ログ構築（3.4のL0規則）→ rd ヒストグラム → **MRC算出（H11判定）**
2. SVMポリシーをターン順にシミュレート: 誕生時π_b判定（(a) oracle: 実際の未来参照を使用＝上界、(b) L0推定＝現実値）、無料窓排出、スタブ・フォルトの再現
3. **キャッシュ完全会計**で再請求額を計算（プレフィックス区間追跡、write/read/missを実料率で。中間不変条件のassert必須）
4. 出力: **Receipt v2** — 実額$57.01 vs シミュ額（oracle / L0）、チャネル別、フォルト回数分布、スキーマ税控除後
限界の明示: シミュレーションは**モデルの挙動変化（品質・順守）を捉えない**。それはV-B/V-Cの担当。
判定: H14。不成立なら**ここで停止**し、ファウンダーへ報告（製品はStage 1止まりの判断材料）。

### V-B A/B実走（D2予算内: 上限$50・V-A合格後）
対応のあるn≥10の小タスク（Autopply/shibubu実バックログ: 失敗テスト修正・小機能）。
arm V=素のClaude Code / arm S=SVM導入済み。同一プロンプト・同一開始状態。
測定: UBベクトル全項（実$、テスト合格、ターン平均レイテンシ、操作変更ゼロの確認、セッション別後悔）。
判定: H15。

### V-C モデル順守検査（V-Bに埋込＋植え込みプローブ）
タスクのうち≥5件に「ページアウト済みブロックが解決に必須」となる状況を意図的に設計。
**フォルト想起率** = 必要時に正しくctx_readで回収した率。最悪事象=回収せず内容を幻覚。
判定: H16（≥90%合格 / 70%未満でアーキテクチャ棄却または スタブ文言再設計→再試験）。
副試験: スタブ文言A/B（想起指示の有無×要約2行vs0行）。

### V-D 無害性・逆風スイート（$0〜数ドル）
SVMに最悪の条件: 短セッション(<30ターン)・同一ファイル高頻度編集・小ブロック多数。
期待: π_b≤0が支配しほぼ何もスタブ化されない。実測コスト差 ≈ スキーマ税のみ。
判定: H17（どのセッションも実費増≤2%）。**成果報酬価格の安全装置。**

### V-E オーケストラ（v2.1・V-B合格後）
Task並列セッションで番地渡し（案E）。comm行（フィクスチャで34.9%だった全状態再放送）の削減率を測る。判定: H9（v1.1から継承）。

---

## §5. 仮説（事前登録・凍結）

| ID | 言明 | 合格しきい値 | 反証時の扱い | 検証 |
|---|---|---|---|---|
| H11 | Context-MRCに安定した膝が存在し、膝<現行常駐量 | 膝 ≤ ウィンドウの40% | 「文脈に局所性なし」→SVM構想棄却 | V-A |
| H14 | シミュ上、L0推定でも実請求≥20%削減（キャッシュ会計込・中間不変） | ΔCost_sim ≥ 20% | 停止・Stage1止まり判断 | V-A |
| H15 | 実走で UB=1（θ_c=15%, θ_s=2pt, θ_l=10%） | UB全項同時成立 | 価格設計を監査固定費型へ後退 | V-B |
| H16 | フォルト想起率 | ≥90%（<70%で棄却） | スタブ再設計→1回だけ再試験 | V-C |
| H17 | 逆風での最大後悔 | ≤2% | 成果報酬不成立→要再設計 | V-D |
| H23 | 価値判定排出が**既定compactionを超えて**家賃を救う（デルタ） | デルタbilled質量 ≥ θ_δ | Stage 2棚上げ（独自層の事業根拠棄却・Stage 1存続） | D-γ |

**反証は失敗ではなく価格防衛である**（v1.1の原則を継承）。特にH17はNoと言えなければならない検証の核。

**H23（D-γ オフライン sim・事前登録・凍結 2026-06-13・ファウンダー承認済）**:

```
win = Σ_b [ 1(価値判定の排出/保持 ≠ 既定compactionの排出/保持) × billed_mass(b) ]

  すなわち利得は「食い違ったブロックの billed 質量」のみ。
  一致した排出は compaction の手柄として利得から除外する。

判定: デルタ billed 質量 ≥ θ_δ（仮 = 総 billed の 10%・D-αのτ_ref同様
      初回実データで較正、初回は仮）→ Stage 2 継続検討
      θ_δ 未満 → Stage 2 棚上げ。Stage 1（監査）は単体で存続。

反証可能性: デルタ≈0 は「Anthropic の compaction で十分」を意味し、
  独自排出層の事業根拠を棄却する。失敗ではなく資源配分の確定。
```

- **設計根拠**: D-αの死因＝環境既存の回復経路（Read=FS）にモデルが投票。compactionも同型の純正機構。D-α後の生存形「無料窓で価値判定つきに排出」は語のまま読めば**価値判定つきcompaction**＝§6リスク③（プラットフォーム吸収）。よって絶対節約（vs無排出）はD-α型楽観の再生産（利得の大半はcompactionが既取得分の二重計上）。デルタのみが「独自層が本当に稼ぐ額」の誠実な定義。デルタ列は「Anthropicが純正でやらないことをどれだけやれるか」＝堀の所在を一つの数で返す。
- **オフライン3点Wave（$0・T9凍結維持・順序固定）**: ① compaction境界で区分較正（`calibrate()` の単調増加仮定を境界分割しR²=0.012を救う）→ ② 既定compactionのkeep/drop集合をログ復元（デルタ基準線）→ ③ 価値判定排出（`decide_policy`/`pi_b`）との差分をbilled質量で採点。
- **較正健全性ゲート（独立先行検証・D-αの土台歪み教訓）**: ①の区分較正R²が回復しない場合（compaction不規則で区分が切れない等）、②③へ進まず**そこで停止し「測定不能」を報告**。garbageな土台にH23を乗せ「デルタ薄→棚上げ」と誤判定するのが最悪。測れないなら「測れない」が正しい停止点。

---

## §6. 実装タスク（順序固定・各受入基準つき）

- **T8 — SVMシミュレータ＋Receipt v2**（即日着手・$0）: mtv-auditに同居、既存パーサ再利用。受入: T1のJSONLでend-to-end、MRC曲線データ出力、oracle/L0/実額の3列表、中間不変assert緑、§9にH11/H14判定を追記。
- **T9 — Session Store＋フック層**: SQLite+FTS5、CAS、PostToolUseスタブ化、SessionStart無料窓、fail-open。受入: p50オーバーヘッド≤15ms、障害注入テストで原文通過、台帳互換。
- **T10 — MCPサーバー**: ctx_read/ctx_search。受入: スキーマ実測<400トークンのテスト、README quickstart、手動デモ（ページアウト→ctx_readで回収）。
- **T11 — A/Bランナー＋植え込みプローブ**（V-B/V-C）: 対応タスク台帳、実費計測、録画可能。上限$50ハードガード（D2準拠）。
- **T12 — 逆風スイート**（V-D）。
- 旧タスクの再配置: 旧T2（リプレイハーネス）はT11後に「出自ゲート付き増分リプレイv2」として再開（発明②）。旧T0（CLAUDE.md分析）はT8のブロック型分類に吸収。旧T4-T7はv1.1のまま後続。

---

## §7. リスク台帳（既知・正直に）

1. **モデル順守**が最大の未知数。H16が本書で唯一「理論で守れない」項目（だからV-Cがある）。
2. **レイテンシ**: フォルト1回≈ツール往復1〜3秒＋1モデルターン。UBのθ_l=10%はこのため。
3. **プラットフォーム吸収**: Anthropicが価値判定つきcompactionを純正搭載するリスク。緩和: クロスプロバイダ設計・台帳/領収書・リポジトリ固有のn̂事前分布（移転不能資産）。
4. **キャッシュTTL**: 5分TTL切れ時の会計はシミュレータで保守側（ミス扱い）に倒す。
5. 本書の数値しきい値は最初の実走で較正余地あり。ただし**変更はファウンダー承認＋版繰上げのみ**（凍結規律）。

---

## §8. Executorの初回指示（本書受領後）

1. 本書全文を読了し、現在地宣言（§8 v1.1準拠）。
2. **T8のみ**に着手し、完了後に以下3点を出して停止: (a) Receipt v2（oracle/L0/実額）、(b) MRC曲線とH11判定、(c) ファウンダーに確認すべき意思決定ちょうど3つ（推奨付き）。
3. T9以降にはH14合格まで触れないこと。ライブAPI支出はT8では発生させないこと。

---

## §9. 実験結果台帳（追記専用）

*（V-A〜V-Eの結果とH11/H14–H17の判定を日付つきで追記。本文書き換え禁止。）*

### 2026-06-12 — V-A シミュレーション監査（T8 完了）

- **対象**: T1 実セッション `7d28a385`（594ターン／アシスタント404、全 opus-4-8、実請求 $57.01）。LLM呼び出しゼロ。
- **実装**: `mtv_audit/svm.py`（既存パーサ再利用）＋ `mtv_audit/svm.py::render_receipt_v2`。Receipt v2 = `reports/svm_receipt_v2_real.md`（real provenance につき git 除外）。テスト `tests/test_svm.py`（11件、全緑。総計37件緑）。
- **較正**: 報告プロンプトトークン P_t ≈ O + α·C_t を OLS で fit → overhead O=18,948 tok（system+tool schema、SVM不変）／est→billed α=3.301／**R²=0.9938**。ベースライン・シミュは実請求を **+8.4%** で再現（プレフィックス・キャッシュ完全会計、**中間不変アサート緑＝breaks 0**）。
- **構造的事実**: 本セッションに **compaction/resume（無料窓）はゼロ**。→ §3.1 系2（無料窓一括排出）は発火せず。フォルトしたページイン本体は中間排出不能（プレフィックス破壊＝§0.2 損失機構）のため終端まで常駐。**全削減は誕生時スタブ化のみに依存**。

判定:

| 仮説 | しきい値 | 実測 | 判定 |
|---|---|---|---|
| **H11** | MRC膝 ≤ 窓の40% | 膝/窓 = 0.03–0.10（全参照メトリックで頑健） | **PASS** |
| **H14** | L0シミュで実請求 ≥ 20%削減（キャッシュ会計込・中間不変） | ΔCost_sim ∈ **[−0.2%, +38.2%]**（閾値20%を帯がまたぐ） | **INCONCLUSIVE** |

- **H14 が解けない根因（単一）**: 大型・受動ブロックの**真のフォルト率**。L0字句メトリックの正規化方法だけで ΔCost の符号が変わる（jaccard: oracle+38.2%/L0+38.0%、block: +20.0%/+2.9%、consumer: +2.0%/−0.2%）。これはモデル挙動量であり V-A では原理的に測れない（§4 V-A 明示的限界、V-C 担当）。
- **回収可能原資は実在**: ページ対象130ブロックの「受動再読家賃」合計 **$21.17 ＝ 実請求の37%**。上位ブロックは寿命数百ターンに対し因果参照 0–4 回。問題は『原資の有無』ではなく『フォルトを誘発せず排出できるか』に集約。
- **H17 先行シグナル**（V-D前倒し・要 T12 確認）: MCPスキーマ常駐コスト ≈380 tok/ターンは T1（大型）では実請求の0.18%と無視可能だが、**極短・低額セッションでは二桁%**を占め最大後悔 ≤2%（H17）を破り得る（テストで実測 13.4%/$0.033セッション）。π_b はホットブロックを誤スタブ化しない（緑）ため、対策はスキーマ側（しきい値超でのみ SVM ツール遅延 attach）。
- **ファウンダー意思決定3点（推奨付き）**: ① **T9+着手前に最小フォルト率プローブ（V-C薄片、≤$5）を回す**（推奨YES／盲目的 T9 着手は非推奨）。② L0 参照メトリック／τ_ref の凍結 = `block` 正規化を既定、τ_ref はプローブ実測で較正（§3隣接につき承認要）。③ **compaction入り・マルチセッションのログをコーパスに追加**（T1は無料窓ゼロ＝最悪条件、系2/V-E のメトリック頑健な利得源は対象外 class に宿る）。
- **次アクション**: §8.3 に従い、H14 が profitable 側に確定するまで T9 以降には着手しない。ファウンダー回答待ち（特に D-α）。

### 2026-06-12 — addendum-A 受領: D-0 完了 ＋ D-α オフライン準備完了

- **D-0（git安全化）完了**: mtv-audit を独立 `git init`（HOME ルート git には書込みゼロ・読取りのみ）。`.gitignore` で `reports/*_real*`・`*.jsonl`・資格情報パターン除外。初回 commit 緑、受入の `git ls-files | grep -E '(env|key|ssh|jsonl|_real)'` **空**。
  - **HOME git 事故は無害と確定**: index 追跡 0 ファイル、全履歴で commit されたのは `README.md` ただ1つ。`.ssh`/`.env`/鍵/履歴は一切 staged されておらず。`.git_HOME_ACCIDENT_BACKUP*` は隔離保持（未削除）。
- **D-α オフライン準備（§B の $0 部分）完了**: `mtv_audit/probe.py`＝B.1 型層化選定／B.2 決定的スタブ（2行・0行）／B.4 フォルト＋幻覚スキャン／B.6 π_b 再計算＋GREEN/AMBER/RED ゲート。テスト `tests/test_probe.py` 7件（総計44件緑）。
  - **プローブ manifest 生成**（`reports/dalpha_manifest_real.json`・real につき git 除外）: 上位30ブロックを型層化し **12ブロック**選定（受動家賃合計 $10.88）。型混在: skill_system 4／search 3／file_read 2／summary 2／other 1（必須4型すべて ≥2）。
  - **副次観察**: 最高家賃ブロック群の多くが**システム/制御テキスト**（`<task-notification>`・plan承認・skill断片、因果参照 0回）。$21 原資の相当部分がユーザ内容でなく制御ノイズ＝スタブ化の最有力候補。
- **B.3 ライブ実走（arm V/S・≤$5）は未着手**: 実 repo 操作＋ライブ課金につきファウンダー go 待ち（実行方式・対象 repo/バックログの確定が必要）。T9 以降は引き続き凍結。

### 2026-06-12 — Runbook 方式確定 ＋ タスク接地 ＋ D-γ ツール限界の発見

- **実走方式 = Runbook 方式**（ファウンダー実走・Executor 採点）、**対象 repo = Shibubu** に確定。`mtv_audit/ctx_server.py`（ctx_read/ctx_search MCP shim、schema 185tok）＋ `mtv_audit/probe.py` 採点駆動 ＋ `D_ALPHA_RUNBOOK.md` ＋ `reports/dalpha_runsheet_real.md`（git除外）。テスト総計50件緑。
- **Autopply ではなく Shibubu**（要確認だった矛盾を解消）: プローブ全12ブロックが Shibubu 由来で、参照ファイル（`pet-ui/.../ChatPanel.tsx`・`HomePage.tsx`(NORMAL_ACTIONS:24/handleAct:147)・`contexts/i18n.tsx`・`pet-mcp/.../rest-chat-image.ts`・`__tests__/chat-emotion.test.ts`）が**全て実在**を確認。content型7タスクを実ファイルにアンカー済み。Autopply バックログでは block↔task の関連性が成立せず実験不能。
- **D-γ 候補ログ発見**（`~/.claude/projects` スキャン）: compaction を持つ最良の同一repo候補 = Shibubu `870c00f2`（asst 1182・compaction 1・10.7MB）。ほか CreativePattern 系に compaction 3–4 のセッション複数。
  - **重要な発見（正直に）**: 現行 T8 シミュレータは **compaction セッションに未対応**。較正 P_t≈O+α·C_t は文脈の単調増加を仮定するが、compaction は文脈を途中リセットするため fit 崩壊（T1 R²=0.994 → `870c00f2` **R²=0.012**）。よって compaction セッションの数値（暫定 oracle +4.7%）は**信頼できず棄却**。**§3.1 系2（無料窓排出＝SVM の最有力メトリック頑健利得源）の評価には、compaction 境界検出＋区分較正＋無料窓排出モデル化の simulator 拡張が必須**。D-γ は「別ログで T8 再走」ではなく simulator 拡張を要する作業（§C 順序どおり D-α 後に着手）。
  - **V-E は観測不能**: スキャンした全セッションで `Task` サブエージェント使用 = **ゼロ**。既存ログからは comm 行（V-E）を測れない。オーケストラ構成の新規セッションが要る。
- **次**: ステップ2（ライブ実走≤$5・Shibubu）はファウンダー手番。Executor は transcript 受領後に採点（§9 追記）。D-γ simulator 拡張は D-α 判定後に別 Wave。T9 は GREEN まで凍結。

### 2026-06-13 — D-α ライブ実走完了（14セル）＋ 採点：判定 AMBER（実測非節約）

- **方式変更（テスト容易化）**: 手動42セッション → ヘッドレス driver（`mtv_audit/dalpha_driver.py`）。`claude -p` 1発=1セル＝自動クリーン文脈。arm 切替は `--mcp-config`＋`--strict-mcp-config` 固定（V=空/S=svm-probe）で V/S 混入を原理的に排除。repo 隔離=セル毎 git worktree＋node_modules symlink（Shibubu 無傷）。`--mock`($0配管検証)/`--only`(単セル) で段階テスト。
- **マトリクス**: content型7タスク×{V,S}×1rep=**14セル**（$5cap 超過のため 3rep→1rep に縮小。Max5x につき実支払いはサブスク内、名目総額 ≈$11）。全14セル rc=0・セッション正常終了。
- **採点の修正（B.4 改訂・live-repo 補正）**: 当初定義「fault = ctx_read(handle)」は live repo で**取りこぼす**。dry-run で arm S は `ctx_read` を呼ばず `Read` で実ファイルを再取得した。よって `probe.py` を **transcript ベース採点**に変更＝本文が ctx_read **または** Read/Grep の tool_result で再入したら fault に算入（ctx_read と disk 再読を区別、幻覚と誤判定しない）。
- **結果**:
  - **f̂(type) = 100%（file_read/search/summary 全型）** — paged-out ブロックは全件、実作業中に再取得された。
  - **フォルト供給源: ctx_read=0件 / disk 再読=7件** — モデルは SVM の想起ツールを**一度も使わず**、7件すべて disk から取り直した。**最重要データ点**。live repo ではファイルシステムが既にページストアであり、Read が既にデマンドページング。新ツール（独自MCP）は既存 Read と冗長競合し、モデルの顕示選好は「ディスクがある、新ツールは要らない」。
  - **幻覚=0件**（健全フォルト率100%・最悪事象ゼロ）。本文捏造で進めたケースなし。
  - **実測 arm コスト: S=$6.83 > V=$4.15（+65%）** — このタスク群では arm S の方が**高い**。
- **判定: AMBER（理論GREEN×実測の乖離）**。π_b 再計算は profitable 99.8%/GREEN（$21.12/$21.17）を出すが、これは**元 T1 セッションの長 residency を母体にした理論側の artifact**。probe タスクは短い（4–16ターン）ため家賃節約 < フル再読コストとなり、実測は非節約。**H14 に対しては「短タスク／live repo では file_read・search・summary いずれも disk 再読で家賃を救えない」という否定寄りの実データ**。
- **測定上の限界（明示）**:
  - **選択バイアス（B.3 設計欠陥）**: 「対象ブロックが本来関与するタスクを選ぶ」と仕様したため、f̂=100% は半ば**構築されたもの**。測ったのは「そのブロックの話をしているとき必要か」（自明にYES）であり、経済が依存する「平均的な未来ターンで必要か」ではない。T1 実データ（数百ターン居座り因果参照0–4）は後者が低いと示唆。よって D-α が確定したのは「**短タスクでスタブ化は損**」までで、「長セッションで家賃が救えない」とまでは言っていない。
  - **success は弱い proxy**: 「セッション正常終了」を success とした（worktree 破棄済みで tsc/test 再検証不可）。**Δsuccess=+0pt は品質同等の証拠にならない**（品質差の検出力なし）。
  - **家賃は$換算で薄い**: 両セッションで文脈の ~98% がキャッシュ読込。$21 プールの節約価値はサブスクではさらに薄く、本物の請求書を持つ API 顧客側でのみ厚い（営業設計に持ち帰り）。
- **生存した設計形**: 介入機構（スタブ＋独自 ctx_read）は重傷。データが示す生存形は小さい——**「無料窓で、価値判定つきで、古いツール結果を消す。パスは残す。新ツールはゼロ。」** モデルの顕示選好（ディスク再読）と Anthropic 純正 context editing プリミティブの双方に整合。ラベル1枚は堀の証明ではない（方向は正しいが証明には程遠い）。
- **次**: D-γ（compaction入り長セッション）。**ただし建てる前に手元の compaction ログで $0 オフライン simulator 拡張だけ回し、現実に聞いてから作る**（D-α の教訓）。D-γ も否定なら Stage 2 棚上げ可（Stage 1＝監査・領収書は単体で成立）。T9 は引き続き凍結。

### 2026-06-13 — D-γ オフライン Wave：win 条件の事前凍結（建てる前）

- **D-α と同型の罠を明示**: D-α が死んだのは「環境が既に持つ回復経路（Read＝FS＝デマンドページング）」をモデルが選び、独自 ctx_read が冗長競合したから。**compaction も同じ立場の「環境が既に積む機構」**。D-α 後に残った生存形「無料窓で価値判定つきに古いツール結果を消す／パスは残す／新ツールゼロ」は**文字どおり『価値判定つき compaction』**であり、§6 リスク③（プラットフォーム吸収）そのもの。よって D-γ は「無料窓排出は家賃を救うか」（naive 比較で自明YES）を問うてはならない。
- **凍結した win 条件（ファウンダー選択＝「デルタ vs 既定compaction」）**: 利得 = **既定 compaction が捨てる/残す集合と、価値判定排出が捨てる/残す集合の食い違い × その billed 質量**。デルタが有意に正でなければ Stage 2 棚上げ（独自層はプラットフォームが既に占める席）。
  - **B.3 再発防止**: メトリックは「排出ブロックが後で未参照」（compaction も同判定＝デルタ0）ではなく、**keep/drop の食い違い件数 × billed 質量**で測る。f̂ は「ブロックが話題の時」ではなく**平均的未来ターン**上で評価。
- **フィージビリティ・ゲート PASS（暫定）**: Shibubu `870c00f2`（3273行・asst1182・compaction1）に `isCompactSummary`/summary がログ内保存。既定 compaction の keep集合（=summary残存）/drop集合（=summary前で要約に入らなかった block）を復元可能 → デルタ測定可能。
- **offline Wave（$0・T9 凍結維持）の3部品**: ① compaction 境界検出＋区分較正（`calibrate()` svm.py:95 の単調増加仮定を境界で分割し R²=0.012 を救う）、② 既定 compaction の keep/drop 集合をログから復元、③ 価値判定排出（既存 `decide_policy`/`pi_b`）との**差分**を billed 質量で採点。判定 GREEN/AMBER/RED は §9 追記。

### 2026-06-13 — D-γ ① 区分較正 実装＋較正健全性ゲート：PASS

- **実装**: `is_compact_summary`（parser が `isCompactSummary` を検出）で session を区分化。compaction 前の累積 est はドロップ（要約で置換）のためリセットし、summary 自身のブロックを次区分のベースラインに。OLS を `_ols()` に切出し、区分別に O/α/R² を fit（`SegmentFit`）。`Calibration.min_segment_r2()` がゲート。compaction 無し session は単一区分＝従来挙動と一致（T1 R² 不変、全50テスト緑）。
- **ゲート結果（Shibubu `870c00f2`・境界1個・pre 791 asst / post 390 asst）**:
  - global 単一 fit **R²=0.012**（前回棄却値）→ 区分較正で **seg1 R²=0.9837 / seg2 R²=0.9591**、**min_segment_R²=0.9591**。目標>0.8 を両区分が大きく上回る → **健全性ゲート PASS**。土台は測定可能。②③へ進む資格成立。
- **正直な発見（②③設計に直結）**: **α が境界で 2.524→0.816（約3倍）ずれた**（O はほぼ同一 87067≈86001＝system+tools+summary ベースライン）。est→billed スケールが phase 依存（推定原因＝sidechain/sub-agent 等で累積est のうち主文脈常駐割合が phase で変化）。**含意：②③の billed 質量は単一 global α でなく per-segment α で計算する**（さもなくばデルタが3倍ぶれ、B.3 同型の「土台歪み→結論が構築物」に陥る）。
- **②③ の framing 精緻化（実装前メモ）**: H23 のデルタは境界 keep/drop の集合差ではなく**時間差**。境界 B で既定 compaction は pre-boundary 全ブロックをドロップ（=SVM と一致）。利得は SVM が低 n̂ ブロックを **B より早く** 排出し、`[t_evict, B)` の家賃を払わない分（－フォルト時の再読コスト）。よって既存 `simulate`/`passive_rent` の「既定排出点＝session 終端」を「＝compaction 境界」に置換した差分が H23。次 Wave。

### 2026-06-13 — D-γ ②③ 実装＋H23 採点：判定 HELD（暫定・α発散で保留）

- **実装**: `mtv_audit/dgamma.py`。②既定compactionのkeep/drop復元＝境界Bでpre-B全ブロックがドロップ（summary置換）。③価値判定排出との食い違い＝「pre-Bで生まれ、B以降に因果参照されたブロック」。グロス質量（H23literal式）とネットデルタ（reread_avoided − extra_rent）の両方を**per-segment α**で算出。テスト4件（総計54件緑）。
- **結果（Shibubu `870c00f2`・境界turn1151）**:
  - 食い違いブロック=**106件、全てseg-1**（分布 {0:106}）。総pageable billed質量=900,032。
  - グロス食い違い質量（literal win）= **27,546**（≈総billedの3.1%）。
  - **ネットデルタ = −24,721（−2.75%）** ＝ **負**。verbatim保持の家賃 > 既定compactionが落として再読させるコスト。θ_δ=10%に対し符号からして未達。**D-αと同方向（独自層が純正を超えない）**。
- **判定: HELD（暫定）**。理由＝α発散（seg1 2.524 / seg2 0.816＝**3.1x**）＋食い違いが単一セグメントに集中。**ネットの符号がα選択に支配される**：extra_rent ∝ billed ∝ α なので、seg-1の高α(2.524)がrentを膨らませ負にしている。もしこれらが真にseg-2の0.816相当なら定数CALL_OVERHEAD(300)が相対的に効いてネットが正に反転し得る。よって「効果が本物か単価アーティファクトか」を切り分け不能 → **α発散の機序を説明するまでGREEN/RED保留**（ファウンダー指示・D-α一般形）。
- **質的サニティ（食い違いの正体）**: 最大は**38日前の stale `<system-reminder>` memory**（refs_after=1, resid_after=82＝1回のために82ターン常駐）。多くはアシスタント自身の長文prose・git-status tool_resultで「B以降に1回だけ・遠方で」参照。**読みとして「compactionが粗く落とした後で稀に必要になった物」＝想定どおりの食い違い**だが、その大半が低頻度・遠方参照＝verbatim保持が高くつく型。デルタが薄い/負である事実と整合。
- **次（α発散の解明＝HELD解除条件）**: seg-1 α=2.524（>1）の機序を特定。仮説：chars/4推定が日本語/コード/JSONを過小評価＋累積estに非常駐分（sidechain等）混入で slope が phase 依存。これを説明・補正してもネットが負なら **H23 は RED 確定→Stage 2 棚上げ、Stage 1 単体存続**。θ_δ=10%は実データ較正待ちのまま。

### 2026-06-13 — D-γ α発散の解明（予言ベース）：(a)(b)棄却 → H23 は instrument-limited で undecided

- **予言ベースで検証（後ろ向き禁止）**。`reports/dgamma_alpha_probe.py`。
  - **予言(a) CJKバイアス → 棄却**: 本sessionのCJK比率は seg-1=4.3% / seg-2=1.6%（ほぼASCII=code/git/英語/path）。事前固定weight（ASCII 0.25・CJK 0.667 tok/char）で測り直してもα=2.524→2.364、**1.0に収束せず**。
  - **予言(b) 非常駐分（sidechain）混入 → 棄却**: `isSidechain` レコード=**0件**。累積est膨張の機構が存在しない。
- **データ直接観察で真因特定（仮説追加でなく分解）**: P/cum が単調減少（13424→2.66、seg-2で2.08→1.00）＝**大定数O（system+tools≈86k）の漸近曲線**。content は **seg-1で tool_result 64%+tool_use 27%、seg-2で tool_result 86%**＝大半が JSON/code/git出力。**chars/4 は英語ASCII調律で、tool-heavy content では kind ごとに誤差が逆向き**：per-turn-delta の kind別fit（R²0.23/0.45・不安定だが方向は出る）で a_toolresult≈0.2（切詰め/冗長で**過大**）、a_tooluse≈4（密JSONで**過小**）、a_text≈3-5。**単一per-segment αを全ブロックに当てる前提が成立しない**。
- **H23 への含意（決定的）**: ネットデルタの符号は **α=1.42 で反転**（net=−22394·α+31800、食い違い106件×CALL_OVERHEAD 300の定数項が効く）。chars/4 では per-block billing scale を 1.42 跨ぎで信頼できるほど pin できない。→ **H23 は RED でも GREEN でもなく instrument-limited で undecided**。これはファウンダーの懸念（「棚上げの根拠の数字自体が怪しい最悪の宙吊り」）を**実証**＝この単一ログ＋chars/4 では H23 を確定できない。
- **un-HOLD 条件（唯一残った道）**: 実トークナイザ（tiktoken or Anthropic count_tokens API、~無料だが**オンライン**＝$0/offline制約を破る＝ファウンダー判断）で per-kind の真billed/estを得る。これだけが undecided → RED/再採点 を分ける。**offline手段は枯渇**（tiktoken/numpy 不在、pip PEP668封鎖）。
- **α非依存の傍証は依然 negative 寄り**: ① D-α 実測ネット負（$11）。② H23 質的サニティ＝食い違いは低頻度・遠方参照（38日stale system-reminderが82ターン常駐で1回参照）でαに依らず verbatim 保持が高くつく型。③「環境が席に座っている」論（D-α=Read/FS、D-γ=compaction）。**H23 の精密値は未確定だが、totality は Stage 2 棚上げ寄り**——ただし clean な RED 数字は得ていない（誇張しない）。

### 2026-06-13 — D-γ 最終測定：実トークナイザで H23 = RED（clean）。Stage 2 棚上げ確定

- **オンライン1回（ファウンダー承認）**: tiktoken o200k（Anthropicトークナイザの proxy）を venv で導入。`reports/dgamma_real_tokenizer.py`。実トークナイザがあれば per-block billed = tokenizer(text) で **α は不要**（α·est が近似しようとしていた当の値）→ 未解明の α を迂回。
- **SANITY（per-kind scale が観察と一致するか・先行確認）**: real density（real_tok/est）は **seg-1=1.712 / seg-2=2.370**。**OLS α（2.524 / 0.816）とどちらも不一致**。→ α は「密度 × residency/truncation」の**交絡した傾き**であり、単一 α を全ブロックに当てる前提が崩れていた直接証拠。per-kind density は tool_result 1.86–2.5・tool_use(JSON) 1.3–1.5・text 1.8–2.2＝全て健全（chars/4 より密、tool-heavy/多言語混在と整合）。測定は信用できる。**α を捨て実トークンを使う判断が正当化された**（先の delta-fit `a_toolresult≈0.2` は density でなく truncation/residency 交絡だったと判明）。
- **判定: RED（clean・α非依存）**: 食い違い106件、総billed（実トークン）=1,288,362。**グロス食い違い質量=20,680＝billedの1.61%**（θ_δ=10% を大きく下回る）。**ネットデルタ=−15,603＝−1.21%（負）**。H23 は二重に RED（グロスですら 1.61%<10%、ネット負）。符号は実トークン直接で **α に依存せず**、proxy 誤差 ±30% でもグロス1.61%は10%に届かず＝**RED は robust**。
- **負の正体（質的・実トークンで確認）**: 上位は resid_after=82/84/95/83 の text/prose と 38日 stale system-reminder＝1回の参照のために数十ターン verbatim 常駐＝再読より高い。
- **オフライン版の HELD を上書き**: 2026-06-13 の D-γ ②③ は est ベースで HELD（α発散で保留）だったが、本オンライン実測が **RED に確定**。HELD の原因（α=2.524 の信頼性）は「α≠density の交絡」と判明し解消。
- **着地（SVM 探索の最終測定）**: **H23 = RED → Stage 2 棚上げ確定。Stage 1（監査・領収書）は単体存続。** SVM の介入機構は live-repo の Read/FS（D-α）にも純正 compaction（D-γ）にも、二度とも「環境が既に席に座っている」ことを実測で確認。$0〜$11＋オンライン1回で買えた確定。診断装置（$57 セッションから $9.41 を実名で指す監査）は一度も揺らがず動いている。**敗北でなく資源配分の確定。**

---
*v2.0 — 凍結日 2026-06-12。v1.1の全凍結事項は有効。本書の数式（§3）・仮説（§5）の変更はファウンダー承認＋版繰上げを要する。*
