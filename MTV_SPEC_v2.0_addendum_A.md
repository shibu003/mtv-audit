# MTV_SPEC_v2.0_addendum_A.md — ファウンダー返信＋D-αプローブ確定仕様

> **Executorへ**: T8の結果を受領した。H11 PASS・較正R²=0.9938・最悪条件サンプルでL0が+2.9%（ほぼ無害）・残る不確実性が単一変数に収束——これは「進め」の報せである。gitを止めた判断は正確だった（D-0で正式対応）。本書は4つの確定決定（D-0〜D-γ）と、D-αプローブの実行可能仕様を与える。**T9以降には引き続き触れないこと。** D-αがprofitable帯を示すまでフック/MCP実装は凍結のままとする。
> v2.0-addendum-A — 2026-06-12 / 凍結規律はv2.0と同じ（§3数式・§5仮説は実行前凍結、結果は§9追記専用）

---

## §A. 確定決定（D-0〜D-γ）

### D-0 — git安全化（最優先・即実行承認）
事故防止が目的。コード判断ではないので承認済みとして着手してよい。
- **mtv-auditフォルダ直下で独立 `git init`**（`~/`ルートのgit設定には書き込まない。読むのみ）。
- `.gitignore` に最低限: `reports/*_real*`・`*.jsonl`・`*.env`・`.env*`・`*_credentials*`・`*.key`・`.ssh/`・`__pycache__/`・`.DS_Store`。実セッション由来物（領収書の生データ含む）と資格情報パターンを最初から除外。
- 誤った `.git_HOME_ACCIDENT_BACKUP*` は**隔離して保持**（削除しない）。HOME配下に実害（追跡された.ssh等）が出ていないかを `git --git-dir=<誤path> status` で**読み取り確認だけ**し、結果を報告。
- 確認後に初回commit。コミットメッセージ英語。
- 受入: mtv-audit が独立repoとして緑、HOMEルートに一切書き込んでいないことの明示確認、機微ファイルが追跡対象外であることの `git ls-files | grep -E '(env|key|ssh|jsonl)'` 空結果。

### D-α — 最小フォルト率プローブ（V-C薄片・≤$5・本書§Bが仕様）
**承認: YES。** H14の符号を決める唯一の変数（大型受動ブロックの真のフォルト率）を$5で実測する。盲目的T9着手より期待情報価値が桁違いに高い。詳細仕様は§B。

### D-β — L0参照メトリック/τ_ref凍結
**D-α実測まで保留**（今は凍結しない）。確定方針: block正規化を既定メトリックとする。τ_ref=0.30は仮のまま据え置き、D-αが出す型別フォルト率データで較正して確定する。D-α後に1行追記で凍結。

### D-γ — コーパス拡張（compaction入り・マルチセッション）
**承認: YES。** T1は無料窓ゼロ＝SVM最悪条件であり、系2（無料窓排出）とV-E（オーケストラ）の利得源が観測されていない。最悪サンプル1本でアーキ全体を棄却しない、という公平性のため。
- 追加対象: Autopply または shibubu.ai のセッションで、次のいずれかを含む1〜2本——(i) compaction/resume を経由（系2が発火しうる）、(ii) Task/サブエージェントを使用（V-E・comm行が出る）、(iii) 200ターン超の長期。
- 扱い: D-α完了後にV-A（T8シミュレータ）を追加ログで再実行し、§9に「class別」で結果併記。**T1単独の数字でverdictを固定しない。**
- ファウンダー作業: 該当JSONLを `~/.claude/projects/` から1〜2本提供（D-α着手と並行で探す）。

---

## §B. D-αプローブ 実行可能仕様

### B.0 目的（1文）
T8シミュレータが「測れない」と正直に棄権した唯一の量——**ページアウトされた大型ブロックが、実タスク遂行中に何回・どの型で `ctx_read` 呼戻しを必要とするか（真のフォルト率）**——を、最小費用の実走で測り、$21原資がprofitable帯に入るか判定する。

### B.1 測定対象ブロックの選定（T8出力から自動抽出）
T8の受動再読家賃ランキング上位から、**型で層化して**選ぶ（型別フォルト率が本プローブの主産物）。
- 上位30ブロックを `type` でグループ化。最低でも次の型を各2件以上含める: `tool_result(file_read)`, `tool_result(grep/search)`, `assistant(調査サマリ/findings)`, `skill/system断片`（例: 領収書#3の原始人プロンプト型）。
- 各ブロックについて T8 から記録: `handle, type, size_tok, birth_turn, lifespan_turns, causal_refs(実測0-4), passive_rent_$`。
- 合計10〜14ブロックに収める（$5制約）。

### B.2 スタブ要約の生成（決定的・本走前にオフラインで）
各対象ブロックに対し、§2スタブ形式の2行要約を**事前生成**（プローブ中のLLM生成を避け、変数を1つに絞る）。
- 要約は「何があるか＋ctx_readの呼び方」のみ。中身の再現はしない。
- D-β較正用に、同一ブロックで **要約2行版** と **要約0行版（番地のみ）** の2種を用意（B.5の副試験用）。

### B.3 タスク設計（対応比較・realバックログ）
- Autopply/shibubu の**実バックログ**から、上記ブロックが本来関与するタスクを n=6〜8 選ぶ（失敗テスト修正・小機能追加・調査依頼など）。合成タスクは使わない（realprovenance維持）。
- 各タスクを2条件で実走、対応比較:
  - **arm V（対照）**: 素のClaude Code。対象ブロックは通常どおり文脈常駐。
  - **arm S（処置）**: 対象ブロックを **B.2のスタブに差し替えた文脈**で開始（手動注入で可。フック実装は不要——これがT9前にできる理由）。`ctx_read` は最小MCPツール1本だけ仮実装、または「ctx_readと書けば該当全文を返す」ワークアラウンドで代替可。
- 同一開始状態・同一プロンプト。モデル・温度固定。各タスク3反復（フォルトは確率的なので）。

### B.4 測定量（フォルト率の定義を凍結）
ブロック単位・タスク単位で記録:
- **フォルト = arm S で、対象ブロックに対し `ctx_read(handle)` が呼ばれた事象**（モデルが全文を必要とした）。
- **フォルト率 f̂(type) = フォルトしたタスク試行数 / 全試行数**、型別に集計（B.0の主産物）。
- **健全フォルト率**: ctx_readで正しく回収できた率（= V-C のフォルト想起率 H16 の薄片）。
- **最悪事象カウント**: 必要なのに ctx_read せず**中身を幻覚した**回数（arm S の出力を arm V およびブロック全文と照合）。1件でも出たら H16 リスクとして赤フラグ。
- **実コスト**: arm V/S 各々の billed トークン・$（較正式 P_t=18948+3.301·C_t を適用）、ターンレイテンシ。
- **成功**: 接地判定（テスト合格 / 期待diff等価）。arm 間で Δsuccess。

### B.5 副試験（D-β較正・追加費用ほぼ0）
arm S 内で B.2の要約2行版 vs 0行版を割り当て、フォルト率と幻覚率の差を見る。
→ 「番地だけで足りるか、2行要約が想起を助けるか」を実測し、スタブ形式とτ_refを確定する。

### B.6 判定ゲート（H14の符号を決める）
T8の π_b 式（§3.2）に **実測 f̂(type)** を `n̂_b` として代入し直し、$21原資のうち何%がprofitable（π_b>0）に残るか再計算する。

| 結果 | 条件 | 次アクション |
|---|---|---|
| **GREEN** | 加重平均 f̂ で原資の≥60%が π_b>0、かつ最悪事象=0、Δsuccess≥−2pt | H14をprofitable側に確定 → T9（フック/MCP）着手を承認 |
| **AMBER** | profitable帯 30–60%、または健全フォルト率<90% | スタブ形式再設計（B.5結果を使う）→ プローブ該当部のみ1回再走 |
| **RED** | profitable<30%、または最悪事象≥1（幻覚） | T9凍結継続。SVMは「無料窓限定排出」のみに縮小 or 監査固定費型へ後退をファウンダーと協議 |

### B.7 受入基準（Executor）
1. 型別フォルト率表 f̂(type) と健全フォルト率・最悪事象カウント。
2. π_b 再計算後の profitable原資% と GREEN/AMBER/RED 判定。
3. B.5 の要約2行 vs 0行 比較（→ D-β確定提案）。
4. 総支出が$5以内であった実証（billed集計）。
5. §9に H14更新・H16薄片・D-β確定案を追記（本文凍結遵守）。
6. **GREEN以外ではT9に着手しないこと。**

---

## §C. 実行順序（確定）
1. **D-0**（git安全化）— 即。これが終わるまで他のcommitをしない。
2. **D-α / §B プローブ** — D-0完了後。$5ハードガード。
3. **D-γ コーパス再実行** — D-α完了後、ファウンダー提供ログで T8再走。
4. 判定がGREENなら **T9** を新規指示で起こす（本書では着手しない）。

---

## §9追記用テンプレ（Executorが結果記入）
```
[2026-06-__] D-α プローブ結果
- f̂(type): file_read=__ grep=__ サマリ=__ skill断片=__
- 健全フォルト率=__%  最悪事象(幻覚)=__件
- π_b再計算: profitable原資=__% / $21.17
- Δsuccess=__pt  Δlatency=__%  総支出=$__
- 判定: GREEN / AMBER / RED
- D-β確定案: 既定メトリック=block正規化, τ_ref=__（較正後）
- H14: [profitable確定 / 縮小 / 後退]
```

---
*v2.0-addendum-A — 2026-06-12。本書はv2.0を拡張する。§3数式・§5仮説の変更はファウンダー承認＋版繰上げを要する。*
